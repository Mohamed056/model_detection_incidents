# Example workflow (non fonctionnel - pour illustration)
# Ce fichier est présent à titre d'exemple pour montrer la structure d'un projet professionnel

name: Example Workflow

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Example step
        run: echo "This is a portfolio project - no CI/CD needed"
